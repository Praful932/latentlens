- contextual embeddings are made available for a set of captions sourced from visual genome captions
- each layers's .pt file contains these contextual embeddings, note that a single token can have multiple 
contextual embeddings, based on the input that it is extracted from

- a set of captions is passed through the llm, at each layer for each token contextualized embeddings are generated
these are saved
- since multiple captions are passed through the llm, there will be same word with a different contextual embedding
    - so pkey will be (layer, sentence, token)
    - one layer embed shape - [300836, 3584] , vocab (note these are contextualized vocab) by embed size

- now through the vlm pass in the image
    - sample n visual tokens and get their embedding at each layer
    - for each visual token, find the top k closest embedding across all layers of the contextualized 
        caption embeddings

- control setup

summary
top1 exact match vs fp16
int8 > nf4

mean depth by layer
at 16th layer, words mapping to visual tokens have a very low min depth, very high level abstractions
behave like a encoder then decoder, high then low then high and quantization effect reverses

hypernym climb kind of present when you comapre int 8 and nf4 alhtough only until layer 16

due to quantization error compounduing, % of exact word match drops

how does a hidden state value change under quantization?


when comparing words against different p, shouldnt top 1 min depth be compared?

inspect manual samples

flash attention enable and needs rerun
add gptq int 8 as well